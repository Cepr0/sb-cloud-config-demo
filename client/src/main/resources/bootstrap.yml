spring:
  application:
    name: demo-client
  main:
    banner-mode: off
  cloud:
    config:
      uri: http://localhost:8080
      fail-fast: true
server:
  forward-headers-strategy: framework
  shutdown: graceful
